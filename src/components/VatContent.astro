<!-- ═══════════════════════════════════════ -->
<!-- ZONE 2: Content below result           -->
<!-- ═══════════════════════════════════════ -->

<!-- ── HOW TO USE ── -->
<div class="content-section reveal">
  <div class="section-divider"></div>
</div>

<section class="content-section reveal">
  <h2>How to Use This Calculator</h2>

  <h3>Add VAT tab</h3>
  <p>Enter the <strong>net amount</strong> (price before VAT) and select the <strong>VAT rate</strong> from the dropdown. The calculator instantly shows the VAT amount and gross total. Use this when you know the ex-VAT price and need to work out the VAT-inclusive figure for an invoice or receipt.</p>

  <h3>Remove VAT tab</h3>
  <p>Enter the <strong>gross amount</strong> (price including VAT) and select the <strong>VAT rate</strong>. The calculator instantly breaks down the net amount and the VAT portion. Use this when you have a VAT-inclusive total and need to extract the VAT — for example, reclaiming input VAT on a business expense.</p>

  <h3>Instant result</h3>
  <p>Results update as you type — no need to press a button. Switch between Add and Remove tabs to calculate in either direction.</p>

  <h3>Share your result</h3>
  <p>Every input is encoded in the URL. Click <strong>Share</strong>, send the link — they'll see your exact numbers. No re-entering, no screenshots.</p>
</section>

<!-- ── FORMULA ── -->
<section class="content-section reveal">
  <h2>The Formula</h2>
  <p>VAT is a percentage added to the net price. To reverse it, divide by 1 plus the rate expressed as a decimal.</p>

  <div class="formula-box">
    <strong>Add VAT:</strong> Gross = Net &times; (1 + Rate)<br><br>
    <strong>Remove VAT:</strong> Net = Gross &divide; (1 + Rate)<br><br>
    <strong>VAT amount:</strong> VAT = Gross &minus; Net<br><br>
    <strong>Shortcut:</strong> VAT at 20% = Gross &divide; 6
  </div>
</section>

<!-- ── EXAMPLE ── -->
<section class="content-section reveal">
  <h2>Example</h2>

  <div class="example-box">
    <h4>Adding VAT to an invoice</h4>
    <p>A freelancer invoices a client for &pound;500 plus VAT at the standard rate of 20%.</p>

    <div class="example-data">
      <div class="example-row"><span class="label">Net amount</span><span class="value">&pound;500.00</span></div>
      <div class="example-row"><span class="label">VAT at 20%</span><span class="value">&pound;100.00</span></div>
      <div class="example-row"><span class="label">Gross total</span><span class="value">&pound;600.00</span></div>
    </div>
  </div>

  <div class="example-box" style="margin-top: 20px;">
    <h4>Removing VAT from a gross amount</h4>
    <p>A receipt shows &pound;600 including VAT at 20%. To find the net price and VAT portion:</p>

    <div class="example-data">
      <div class="example-row"><span class="label">Gross amount</span><span class="value">&pound;600.00</span></div>
      <div class="example-row"><span class="label">Net amount</span><span class="value">&pound;500.00</span></div>
      <div class="example-row"><span class="label">VAT</span><span class="value">&pound;100.00</span></div>
    </div>
  </div>

  <div class="example-box" style="margin-top: 20px;">
    <h4>Reduced rate — energy bill</h4>
    <p>A &pound;1,000 domestic energy bill includes VAT at the reduced rate of 5%.</p>

    <div class="example-data">
      <div class="example-row"><span class="label">Gross amount</span><span class="value">&pound;1,000.00</span></div>
      <div class="example-row"><span class="label">Net amount</span><span class="value">&pound;952.38</span></div>
      <div class="example-row"><span class="label">VAT at 5%</span><span class="value">&pound;47.62</span></div>
    </div>
  </div>
</section>

<!-- ── FAQ ── -->
<section class="content-section reveal">
  <h2>FAQ</h2>

  <div class="faq-item">
    <button class="faq-question" aria-expanded="false">
      What's the VAT registration threshold?
      <span class="faq-chevron">&#9660;</span>
    </button>
    <div class="faq-answer">
      <div class="faq-answer-inner">
        The VAT registration threshold is &pound;90,000 from April 2024. If your taxable turnover exceeds this amount over any rolling 12-month period, you must register for VAT with HMRC. You can also register voluntarily below the threshold if you want to reclaim input VAT on purchases.
      </div>
    </div>
  </div>

  <div class="faq-item">
    <button class="faq-question" aria-expanded="false">
      What items are zero-rated?
      <span class="faq-chevron">&#9660;</span>
    </button>
    <div class="faq-answer">
      <div class="faq-answer-inner">
        Most food and drink (excluding restaurant meals, alcohol, and confectionery), children's clothing and footwear, books and newspapers, and public transport are zero-rated. Zero-rated means VAT is charged at 0% — the items are still taxable, but the rate is nil.
      </div>
    </div>
  </div>

  <div class="faq-item">
    <button class="faq-question" aria-expanded="false">
      What's the difference between zero-rated and exempt?
      <span class="faq-chevron">&#9660;</span>
    </button>
    <div class="faq-answer">
      <div class="faq-answer-inner">
        Zero-rated goods and services are technically taxable at 0%, which means the business can still reclaim input VAT on related purchases. Exempt supplies (such as financial services, education, and health services) are outside the VAT system entirely — no VAT is charged, but the business cannot reclaim input VAT on costs related to those exempt supplies.
      </div>
    </div>
  </div>

  <div class="faq-item">
    <button class="faq-question" aria-expanded="false">
      How do I calculate VAT backwards?
      <span class="faq-chevron">&#9660;</span>
    </button>
    <div class="faq-answer">
      <div class="faq-answer-inner">
        To extract VAT from a gross (VAT-inclusive) price, divide by 1 plus the VAT rate as a decimal. For the standard rate of 20%, divide the gross amount by 1.20 to get the net price. For the reduced rate of 5%, divide by 1.05. The VAT amount is the difference between gross and net. A handy shortcut for 20% VAT: divide the gross by 6 to get the VAT amount directly.
      </div>
    </div>
  </div>
</section>

<!-- ── RELATED CALCS ── -->
<section class="content-section reveal">
  <h2>Related Calculators</h2>
  <div class="related-grid">
    <a href="/uk/income-tax-calculator" class="related-card">
      <div class="related-card-title">Income Tax Calculator</div>
      <div class="related-card-desc">Calculate your UK income tax and National Insurance contributions.</div>
    </a>
    <a href="/uk/take-home-pay-calculator" class="related-card">
      <div class="related-card-title">Take-Home Pay Calculator</div>
      <div class="related-card-desc">See your actual take-home pay after tax and NI deductions.</div>
    </a>
    <a href="/uk/stamp-duty-calculator" class="related-card">
      <div class="related-card-title">Stamp Duty Calculator</div>
      <div class="related-card-desc">Work out how much stamp duty you'll pay on a property purchase.</div>
    </a>
  </div>
</section>

<!-- ── EMBED ── -->
<div class="embed-section reveal">
  <h3>Add This Calculator to Your Website</h3>
  <p>Embed the sum.money VAT calculator on your site. Free, responsive, always up-to-date.</p>
  <div class="embed-code">&lt;iframe src="https://sum.money/embed/uk/vat-calculator" width="100%" height="500"&gt;&lt;/iframe&gt;</div>
</div>

<!-- ── FAQ Accordion + Scroll Reveal scripts ── -->
<script is:inline>
// FAQ accordion
document.querySelectorAll('.faq-question').forEach(function(q) {
  q.addEventListener('click', function() {
    var item = q.parentElement;
    var answer = item.querySelector('.faq-answer');
    var isOpen = item.classList.contains('open');

    document.querySelectorAll('.faq-item').forEach(function(fi) {
      fi.classList.remove('open');
      fi.querySelector('.faq-answer').style.maxHeight = '0';
      fi.querySelector('.faq-question').setAttribute('aria-expanded', 'false');
    });

    if (!isOpen) {
      item.classList.add('open');
      answer.style.maxHeight = answer.scrollHeight + 'px';
      q.setAttribute('aria-expanded', 'true');
    }
  });
});

// Scroll reveal
var observer = new IntersectionObserver(function(entries) {
  entries.forEach(function(e) {
    if (e.isIntersecting) {
      e.target.classList.add('visible');
      observer.unobserve(e.target);
    }
  });
}, { threshold: 0.1, rootMargin: '0px 0px -40px 0px' });

document.querySelectorAll('.reveal').forEach(function(el) { observer.observe(el); });

// Track related calc clicks
document.querySelectorAll('.related-card').forEach(function(card) {
  card.addEventListener('click', function() {
    try {
      var payload = { event: 'related_click', timestamp: Date.now(), target: card.getAttribute('href'), url: location.pathname };
      if (navigator.sendBeacon) navigator.sendBeacon('/api/track', JSON.stringify(payload));
    } catch(e) {}
  });
});
</script>
